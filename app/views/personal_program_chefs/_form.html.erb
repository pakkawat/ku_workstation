<%= form_for(@personal_program_chef) do |f| %>
  <% if @personal_program_chef.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@personal_program_chef.errors.count, "error") %> prohibited this personal_program_chef from being saved:</h2>

      <ul>
      <% @personal_program_chef.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :personal_chef_resource_id %><br>
    <%= f.number_field :personal_chef_resource_id %>
  </div>
  <div class="field">
    <%= f.label :personal_program_id %><br>
    <%= f.number_field :personal_program_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
