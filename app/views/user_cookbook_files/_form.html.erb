<%= form_for(@user_cookbook_file) do |f| %>
  <% if @user_cookbook_file.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@user_cookbook_file.errors.count, "error") %> prohibited this user_cookbook_file from being saved:</h2>

      <ul>
      <% @user_cookbook_file.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
